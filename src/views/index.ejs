<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <title>Mongo CRUD index</title>
</head>
<body>
    <div class="container">
        <h1>Task Manager landing page</h1>

        <form action="/add" method="post">
            <legend>
                Create new task
            </legend>
            <div class="form-group">
                <input type="text" id="title" class="form-control" name="Title"/>
            </div>
            <br>
            <div class="form-group">
                <textarea class="form-control" rows="1" cols="50" name="Description" placeholder="Add a description"></textarea>
            </div>
            <br>
            <button class="btn btn-primary">
                Add task
            </button>
        </form>
    </div>
    <div class="container">
        <hr/>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
                <%for (var i = 0; i < tasks.length; i++) {%>
                    <tr>
                        <td><%= i + 1%></td>
                        <td>
                            <strong><%= tasks[i].Title %></strong>
                        </td>
                        <td><%= tasks[i].Description %></td>
                        <td> 
                            <a 
                            class="<%= tasks[i].Status ? 'btn btn-success': 'btn btn-dark' %>"
                            href="/changeStatus/<%= tasks[i]._id %>">Done</a>
                            <a 
                            class="btn btn-danger"
                            id="delete"
                            href="/delete/<%= tasks[i]._id %>">Delete task</a>
                        </td>
                    </tr>
                <% }%>
            </tbody>
        </table>
    </div>

    <script>
        document.getElementById('title').focus();
    </script>
</body>
</html>